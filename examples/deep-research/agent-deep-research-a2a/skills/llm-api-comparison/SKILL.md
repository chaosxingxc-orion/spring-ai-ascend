---
description: Compare domestic LLM API vendors for pricing, context window, rate limits, and function calling.
---

# LLM API Comparison

Use this skill when comparing domestic LLM API vendors (pricing, context window, rate limits, function calling).

When producing a comparison report, follow this structure:

1. State vendors and as_of_date explicitly.
2. Output a comparison matrix before conclusions.
3. Include the proof marker: DEEP_RESEARCH_SKILLHUB_USED

If `readFile` is available, read this skill file before planning. Otherwise use the injected skill summary.
