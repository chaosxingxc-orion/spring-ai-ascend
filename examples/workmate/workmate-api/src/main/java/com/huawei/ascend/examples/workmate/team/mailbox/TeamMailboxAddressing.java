package com.huawei.ascend.examples.workmate.team.mailbox;

/** Well-known mailbox sender ids for W52 team routing. */
public final class TeamMailboxAddressing {

    /** User {@code @member} bypass messages (distinct from the team lead internal id). */
    public static final String USER_SENDER_ID = "__user__";

    private TeamMailboxAddressing() {}
}
