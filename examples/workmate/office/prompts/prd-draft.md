Write a product requirements draft as prd-draft.md in the session workspace.

Use your write tool. Structure the document with these sections:

1. Background — WorkMate Workbench MVP (local JVM agent + workspace)
2. Goals — session tasks, HITL for risky bash, MCP doc read
3. User stories — PM writes PRD draft; engineer runs dogfood scripts
4. Out of scope — cloud sandbox, Electron (v0.2+)
5. Acceptance — three dogfood scripts pass

Keep it concise (about 40–80 lines markdown). Do not create any other files.
