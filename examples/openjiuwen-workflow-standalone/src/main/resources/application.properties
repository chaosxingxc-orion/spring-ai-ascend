# OpenJiuwen Workflow Standalone Example — LLM Configuration
# Environment variables (SAA_*) override these defaults.

model.provider=openai
model.api-key=sk-your-api-key-here
model.api-base=http://localhost:4000/v1
model.name=gpt-4
model.temperature=0.7
model.max-tokens=1024
model.ssl-verify=true
