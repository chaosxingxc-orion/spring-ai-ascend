---
level: L1
view: process
module: agent-service
status: skeleton
authority: "ADR-0138 (rc53 — 5-layer L1 ratification) + ADR-0139 (rc53 — Fast/Slow Path narrowed semantics) + ADR-0142 (rc55 — Run aggregate single owner; Layer 4 invokes Layer 2 via RunRepository CAS) + ADR-0145 (rc55 — sealed RunEvent emissions per scenario)"
---

# agent-service — Process View

> Wave: rc55 W2 skeleton. Content arrives in rc55 W4.
> Authoring source: review file `docs/logs/reviews/2026-05-26-agent-service-l1-4plus1-rewrite-wave-1.en.md` §16, ported with rc55 corrections (R2 design_only mark, M7 DualTrackRouter annotation, O3 cancel-race-loser sequence, R3 Layer-4-invokes-Layer-2 aggregate pattern, RunEvent emission annotations per ADR-0145) per the rc55 plan.

## 1. P1 — Standard Synchronous Intake → State Machine → Suspend → Resume (covers S1 + S2)

TODO Wave 4.

## 2. P2 — Fast-Path / Slow-Path Decision Tree (covers S1 / S2 divergence)

TODO Wave 4. Includes M7 `(design_only — W2 ADR-0112)` annotation on DualTrackRouter / SlowTrackJudge.

## 3. P3 — Cancel During Execution + Re-auth + Cancel Race Winner (covers S5 winner side)

TODO Wave 4.

## 4. P4 — A2A Peer Collaboration (covers S3)

TODO Wave 4.

## 5. P5 — S2C Client Callback (covers S4)

TODO Wave 4.

## 6. P6 — Cancel Race LOSER Sequence (O3 — new in rc55)

TODO Wave 4.
