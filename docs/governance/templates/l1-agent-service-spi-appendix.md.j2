---
level: L1
view: development
module: agent-service
status: skeleton
authority: "ADR-0099 (rc22 — Rule G-1.1.b SPI Appendix 4-way parity) + Rule R-D (SPI + DFX + TCK co-design + catalog integrity)"
---

# agent-service — SPI Interface Appendix

> Wave: rc55 W2 skeleton. Content arrives in rc55 W5.
> Authoring source: agent-service/ARCHITECTURE.md §SPI Interface Appendix + review file §19, ported with rc55 corrections (M4 TaskStateStore naming, M10 memory.spi carrier audit per sibling-sweep findings).
> 4-way parity gate per Rule G-1.1.b: FQN appears in `agent-service/module-metadata.yaml#spi_packages` + `docs/contracts/contract-catalog.md` §2 Active SPI interfaces + `docs/dfx/agent-service.yaml#spi_packages` + on-disk `.java` file.

## 1. Active Java SPI interfaces (9 — Wave 5 verifies 4-way parity)

TODO Wave 5.

## 2. SPI-adjacent structural carriers

TODO Wave 5. Per Rule R-D.d (.spi packages contain interfaces only); carriers listed separately and NOT counted in the 9-SPI total.

## 3. Cross-module SPI consumption

TODO Wave 5. Lists which SPIs agent-service consumes from peer modules
(agent-execution-engine, agent-bus, agent-middleware) per the dependency
allowlist in `module-metadata.yaml#allowed_dependencies`.

## 4. Future SPIs (design_only, declared but not yet on disk)

TODO Wave 5. Includes the `agent.spi.Agent` + `agent.spi.AgentRegistry` design_only entries
+ the future sealed `RunEvent` hierarchy per ADR-0145 (10 variants
specified in `docs/contracts/run-event.v1.yaml`; Java sealed type lands
in follow-up impl-mode wave).
